from tqdm import tqdm

def main(args):
    pass

    # create model

    # train model
    for epoch_i in tqdm(range(args.num_epochs)):
        pass
        # run iter of model
        # differentiable rasterization
        # color and depth loss